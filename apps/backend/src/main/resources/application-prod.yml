spring:
  jpa:
    hibernate:
      ddl-auto: validate

piip:
  security:
    allowed-origins: ${PIIP_ALLOWED_ORIGINS}
  documents:
    max-size-bytes: ${PIIP_DOCUMENT_MAX_SIZE}
  audit:
    retention-days: ${PIIP_AUDIT_RETENTION_DAYS}
