/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';

import { BaseService } from '../base-service';
import { ApiConfiguration } from '../api-configuration';
import { StrictHttpResponse } from '../strict-http-response';

import { CurrentUserResponse } from '../models/current-user-response';
import { me } from '../fn/identity-controller/me';
import { Me$Params } from '../fn/identity-controller/me';

@Injectable({ providedIn: 'root' })
export class IdentityControllerService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http);
  }

  /** Path part for operation `me()` */
  static readonly MePath = '/identity/me';

  /**
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `me()` instead.
   *
   * This method doesn't expect any request body.
   */
  me$Response(params?: Me$Params, context?: HttpContext): Observable<StrictHttpResponse<CurrentUserResponse>> {
    const obs = me(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `me$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  me(params?: Me$Params, context?: HttpContext): Observable<CurrentUserResponse> {
    const resp = this.me$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<CurrentUserResponse>): CurrentUserResponse => r.body)
    );
  }

}
