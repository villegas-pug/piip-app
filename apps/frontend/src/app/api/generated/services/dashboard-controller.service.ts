/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { Observable } from 'rxjs';
import { map } from 'rxjs/operators';

import { BaseService } from '../base-service';
import { ApiConfiguration } from '../api-configuration';
import { StrictHttpResponse } from '../strict-http-response';

import { DashboardResponse } from '../models/dashboard-response';
import { summary } from '../fn/dashboard-controller/summary';
import { Summary$Params } from '../fn/dashboard-controller/summary';

@Injectable({ providedIn: 'root' })
export class DashboardControllerService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http);
  }

  /** Path part for operation `summary()` */
  static readonly SummaryPath = '/dashboard';

  /**
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `summary()` instead.
   *
   * This method doesn't expect any request body.
   */
  summary$Response(params?: Summary$Params, context?: HttpContext): Observable<StrictHttpResponse<DashboardResponse>> {
    const obs = summary(this.http, this.rootUrl, params, context);
    return obs;
  }

  /**
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `summary$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  summary(params?: Summary$Params, context?: HttpContext): Observable<DashboardResponse> {
    const resp = this.summary$Response(params, context);
    return resp.pipe(
      map((r: StrictHttpResponse<DashboardResponse>): DashboardResponse => r.body)
    );
  }

}
