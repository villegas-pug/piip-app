/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ScopeResponse } from '../../models/scope-response';

export interface Reactivate$Params {
  scopeId: number;
  version: number;
}

export function reactivate(http: HttpClient, rootUrl: string, params: Reactivate$Params, context?: HttpContext): Observable<StrictHttpResponse<ScopeResponse>> {
  const rb = new RequestBuilder(rootUrl, reactivate.PATH, 'put');
  if (params) {
    rb.path('scopeId', params.scopeId, {});
    rb.query('version', params.version, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ScopeResponse>;
    })
  );
}

reactivate.PATH = '/admin/role-assignments/{scopeId}/reactivation';
