/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { UserAssignmentCandidateResponse } from '../../models/user-assignment-candidate-response';

export interface AssignmentCandidates$Params {
}

export function assignmentCandidates(http: HttpClient, rootUrl: string, params?: AssignmentCandidates$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<UserAssignmentCandidateResponse>>> {
  const rb = new RequestBuilder(rootUrl, assignmentCandidates.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<UserAssignmentCandidateResponse>>;
    })
  );
}

assignmentCandidates.PATH = '/admin/users/assignment-candidates';
