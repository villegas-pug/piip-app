/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { HomePortfolioResponse } from '../../models/home-portfolio-response';

export interface Portfolio$Params {
  executingUnitId: number;
  q?: string;
  type?: 'INITIATIVE' | 'PROJECT';
  status?: 'PRESENTED' | 'INITIATIVE_APPROVED' | 'INITIATIVE_ARCHIVED' | 'PROJECT_IN_PROGRESS' | 'PRODUCT_APPROVED' | 'PRODUCT_NOT_APPROVED' | 'SUSPENDED' | 'CANCELLED' | 'FINISHED' | 'NOT_APPLICABLE' | 'NOT_ADMISSIBLE';
  page?: number;
  size?: number;
}

export function portfolio(http: HttpClient, rootUrl: string, params: Portfolio$Params, context?: HttpContext): Observable<StrictHttpResponse<HomePortfolioResponse>> {
  const rb = new RequestBuilder(rootUrl, portfolio.PATH, 'get');
  if (params) {
    rb.query('executingUnitId', params.executingUnitId, {});
    rb.query('q', params.q, {});
    rb.query('type', params.type, {});
    rb.query('status', params.status, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<HomePortfolioResponse>;
    })
  );
}

portfolio.PATH = '/dashboard/portfolio';
