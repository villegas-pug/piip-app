/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { VersionResponse } from '../../models/version-response';

export interface AddVersionToFile$Params {
  recordCode: string;
  fileId: number;
      body?: {
'file': Blob;
}
}

export function addVersionToFile(http: HttpClient, rootUrl: string, params: AddVersionToFile$Params, context?: HttpContext): Observable<StrictHttpResponse<VersionResponse>> {
  const rb = new RequestBuilder(rootUrl, addVersionToFile.PATH, 'post');
  if (params) {
    rb.path('recordCode', params.recordCode, {});
    rb.path('fileId', params.fileId, {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<VersionResponse>;
    })
  );
}

addVersionToFile.PATH = '/portfolio-records/{recordCode}/documents/files/{fileId}/versions';
