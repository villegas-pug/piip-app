/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { VersionResponse } from '../../models/version-response';

export interface Publication$Params {
  recordCode: string;
  versionId: number;
  published: boolean;
  version: number;
}

export function publication(http: HttpClient, rootUrl: string, params: Publication$Params, context?: HttpContext): Observable<StrictHttpResponse<VersionResponse>> {
  const rb = new RequestBuilder(rootUrl, publication.PATH, 'put');
  if (params) {
    rb.path('recordCode', params.recordCode, {});
    rb.path('versionId', params.versionId, {});
    rb.query('published', params.published, {});
    rb.query('version', params.version, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<VersionResponse>;
    })
  );
}

publication.PATH = '/portfolio-records/{recordCode}/documents/versions/{versionId}/publication';
