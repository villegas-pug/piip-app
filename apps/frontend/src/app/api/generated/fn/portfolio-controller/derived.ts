/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DerivedProjectRequest } from '../../models/derived-project-request';
import { PortfolioRecordResponse } from '../../models/portfolio-record-response';

export interface Derived$Params {
      body: DerivedProjectRequest
}

export function derived(http: HttpClient, rootUrl: string, params: Derived$Params, context?: HttpContext): Observable<StrictHttpResponse<PortfolioRecordResponse>> {
  const rb = new RequestBuilder(rootUrl, derived.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PortfolioRecordResponse>;
    })
  );
}

derived.PATH = '/projects/derived';
