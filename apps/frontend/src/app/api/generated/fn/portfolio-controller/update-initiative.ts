/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { InitiativeUpdateRequest } from '../../models/initiative-update-request';
import { PortfolioRecordResponse } from '../../models/portfolio-record-response';

export interface UpdateInitiative$Params {
  code: string;
      body: InitiativeUpdateRequest
}

export function updateInitiative(http: HttpClient, rootUrl: string, params: UpdateInitiative$Params, context?: HttpContext): Observable<StrictHttpResponse<PortfolioRecordResponse>> {
  const rb = new RequestBuilder(rootUrl, updateInitiative.PATH, 'patch');
  if (params) {
    rb.path('code', params.code, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PortfolioRecordResponse>;
    })
  );
}

updateInitiative.PATH = '/initiatives/{code}';
