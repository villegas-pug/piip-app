/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PortfolioRecordResponse } from '../../models/portfolio-record-response';
import { PreexistingProjectRequest } from '../../models/preexisting-project-request';

export interface Preexisting$Params {
      body: PreexistingProjectRequest
}

export function preexisting(http: HttpClient, rootUrl: string, params: Preexisting$Params, context?: HttpContext): Observable<StrictHttpResponse<PortfolioRecordResponse>> {
  const rb = new RequestBuilder(rootUrl, preexisting.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PortfolioRecordResponse>;
    })
  );
}

preexisting.PATH = '/projects/preexisting';
