/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PortfolioRecordResponse } from '../../models/portfolio-record-response';

export interface Initiative$Params {
  code: string;
}

export function initiative(http: HttpClient, rootUrl: string, params: Initiative$Params, context?: HttpContext): Observable<StrictHttpResponse<PortfolioRecordResponse>> {
  const rb = new RequestBuilder(rootUrl, initiative.PATH, 'get');
  if (params) {
    rb.path('code', params.code, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PortfolioRecordResponse>;
    })
  );
}

initiative.PATH = '/initiatives/{code}';
