/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponsePortfolioRecordResponse } from '../../models/page-response-portfolio-record-response';

export interface Projects$Params {
  q?: string;
  status?: string;
  executingUnitId?: number;
  page?: number;
  size?: number;
}

export function projects(http: HttpClient, rootUrl: string, params?: Projects$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponsePortfolioRecordResponse>> {
  const rb = new RequestBuilder(rootUrl, projects.PATH, 'get');
  if (params) {
    rb.query('q', params.q, {});
    rb.query('status', params.status, {});
    rb.query('executingUnitId', params.executingUnitId, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponsePortfolioRecordResponse>;
    })
  );
}

projects.PATH = '/projects';
