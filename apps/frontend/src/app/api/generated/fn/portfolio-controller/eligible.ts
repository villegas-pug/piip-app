/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PortfolioRecordResponse } from '../../models/portfolio-record-response';

export interface Eligible$Params {
}

export function eligible(http: HttpClient, rootUrl: string, params?: Eligible$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<PortfolioRecordResponse>>> {
  const rb = new RequestBuilder(rootUrl, eligible.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<PortfolioRecordResponse>>;
    })
  );
}

eligible.PATH = '/projects/eligible-initiatives';
