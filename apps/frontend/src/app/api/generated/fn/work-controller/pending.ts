/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TaskResponse } from '../../models/task-response';

export interface Pending$Params {
}

export function pending(http: HttpClient, rootUrl: string, params?: Pending$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<TaskResponse>>> {
  const rb = new RequestBuilder(rootUrl, pending.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<TaskResponse>>;
    })
  );
}

pending.PATH = '/work-tasks';
