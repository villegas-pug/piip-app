/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OrganizationalUnitResponse } from '../../models/organizational-unit-response';

export interface OrganizationalUnits$Params {
  executingUnitId: number;
}

export function organizationalUnits(http: HttpClient, rootUrl: string, params: OrganizationalUnits$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<OrganizationalUnitResponse>>> {
  const rb = new RequestBuilder(rootUrl, organizationalUnits.PATH, 'get');
  if (params) {
    rb.query('executingUnitId', params.executingUnitId, {});
  }

  return http.request(
    rb.build({ responseType: 'blob', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<OrganizationalUnitResponse>>;
    })
  );
}

organizationalUnits.PATH = '/organizational-units';
