/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { InstitutionResponse } from '../../models/institution-response';

export interface Institutions1$Params {
}

export function institutions1(http: HttpClient, rootUrl: string, params?: Institutions1$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<InstitutionResponse>>> {
  const rb = new RequestBuilder(rootUrl, institutions1.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<InstitutionResponse>>;
    })
  );
}

institutions1.PATH = '/admin/organization/institutions';
