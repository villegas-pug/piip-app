/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ExecutingUnitResponse } from '../../models/executing-unit-response';

export interface DeactivateExecutingUnit$Params {
  id: number;
  version: number;
}

export function deactivateExecutingUnit(http: HttpClient, rootUrl: string, params: DeactivateExecutingUnit$Params, context?: HttpContext): Observable<StrictHttpResponse<ExecutingUnitResponse>> {
  const rb = new RequestBuilder(rootUrl, deactivateExecutingUnit.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('version', params.version, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ExecutingUnitResponse>;
    })
  );
}

deactivateExecutingUnit.PATH = '/admin/organization/executing-units/{id}/deactivation';
