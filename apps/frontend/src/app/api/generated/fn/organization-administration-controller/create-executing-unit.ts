/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateExecutingUnitRequest } from '../../models/create-executing-unit-request';
import { ExecutingUnitResponse } from '../../models/executing-unit-response';

export interface CreateExecutingUnit$Params {
  institutionId: number;
      body: CreateExecutingUnitRequest
}

export function createExecutingUnit(http: HttpClient, rootUrl: string, params: CreateExecutingUnit$Params, context?: HttpContext): Observable<StrictHttpResponse<ExecutingUnitResponse>> {
  const rb = new RequestBuilder(rootUrl, createExecutingUnit.PATH, 'post');
  if (params) {
    rb.query('institutionId', params.institutionId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ExecutingUnitResponse>;
    })
  );
}

createExecutingUnit.PATH = '/admin/organization/executing-units';
