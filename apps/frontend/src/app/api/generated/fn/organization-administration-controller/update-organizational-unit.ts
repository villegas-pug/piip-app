/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { OrganizationalUnitResponse } from '../../models/organizational-unit-response';
import { UpdateOrganizationalUnitRequest } from '../../models/update-organizational-unit-request';

export interface UpdateOrganizationalUnit$Params {
  id: number;
  version: number;
      body: UpdateOrganizationalUnitRequest
}

export function updateOrganizationalUnit(http: HttpClient, rootUrl: string, params: UpdateOrganizationalUnit$Params, context?: HttpContext): Observable<StrictHttpResponse<OrganizationalUnitResponse>> {
  const rb = new RequestBuilder(rootUrl, updateOrganizationalUnit.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.query('version', params.version, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<OrganizationalUnitResponse>;
    })
  );
}

updateOrganizationalUnit.PATH = '/admin/organization/organizational-units/{id}';
